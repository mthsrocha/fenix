version: '3'

services:
    postgres:
        image: postgres:latest
        ports:
        - 5432:5432
        environment:
            POSTGRES_DB: 'mak'
            POSTGRES_USER: 'root'
            POSTGRES_PASSWORD: 'root'

        container_name: mak_db